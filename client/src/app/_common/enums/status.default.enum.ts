export enum StatusDefault {
  DISABLED,
  ENABLED,
}

export const statusDefaultArray = [
  { key: StatusDefault.DISABLED, label: 'Deactivated' },
  { key: StatusDefault.ENABLED, label: 'Activated' },
]
