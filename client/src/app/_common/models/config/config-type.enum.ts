export enum ConfigType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  NUMBER = 'number',
  DECIMAL = 'decimal',
  SELECT = 'select',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  PASSWORD = 'password',
}
