export enum ConfigType {
  TEXT = "text",
  TEXTAREA = "textarea",
  NUMBER = "number",
  DECIMAL = "decimal",
  SELECT = "select",
  CHECKBOX = "checkbox",
  RADIO = "radio",
  PASSWORD = "password",
}
